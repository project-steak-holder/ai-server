# Database Configuration
# Neon PostgreSQL connection string
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@project.neon.tech/dbname?sslmode=require

# AI Provider Configuration
# API key for AI provider (OpenAI, Anthropic, or other supported providers)
AI_PROVIDER_API_KEY=your-api-key-here
# Base URL for AI provider (optional, uses provider default if not set)
AI_PROVIDER_BASE_URL=your-provider-base-url-here
# Model name or identifier for the AI provider (e.g. gpt-4o-mini, claude-2)
AI_PROVIDER_MODEL=your-model-name-here

# Neon Auth base URL (used by Neon Auth SDK / Data API)
# Example: https://auth.neon.tech or the value shown in the Neon Console
AUTH_URL=your-neon-auth-base-url-here